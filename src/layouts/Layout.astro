---
// src/layouts/Layout.astro
import  SpeedInsights  from "@vercel/speed-insights/astro";
---

<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Portfolio Nixon Cruz</title>
    <slot name="head" />
  </head>

  <body>
    <slot />

    <!-- Speed Insights: métrica de rendimiento de Vercel -->
    <SpeedInsights />
  </body>
</html>

<style is:global>
	/* Variables CSS */
	:root {
		--color-primary: #9333ea;
		--color-secondary: #bc52ee;
		--color-text: #ffffff;
		--color-text-light: #e2e2e2;
		--color-background: #1a0b2e;
		--color-section-border: rgba(147, 51, 234, 0.2);
	}

	* {
		margin: 0;
		padding: 0;
		box-sizing: border-box;
	}

	html,
	body {
		width: 100%;
		height: 100%;
		font-family: Inter, Roboto, 'Helvetica Neue', Arial, sans-serif;
		color: var(--color-text);
	}

	body {
		background: 
			/* Capas de gradiente para profundidad */
			radial-gradient(circle at 20% 30%, rgba(147, 51, 234, 0.15), transparent 40%),
			radial-gradient(circle at 80% 70%, rgba(188, 82, 238, 0.12), transparent 40%),
			/* Gradiente base */
			linear-gradient(135deg, 
				#1a0b2e 0%,
				#271344 25%,
				#321b5a 50%,
				#271344 75%,
				#1a0b2e 100%
			);
		background-attachment: fixed;
		min-height: 100vh;
	}

	/* Efecto de grano sutil */
	body::before {
		content: '';
		position: fixed;
		top: 0;
		left: 0;
		width: 100%;
		height: 100%;
		background-image: 
			repeating-linear-gradient(0deg, transparent, transparent 2px, rgba(255,255,255,0.02) 2px, rgba(255,255,255,0.02) 4px);
		pointer-events: none;
		opacity: 0.2;
		z-index: 1;
	}

	/* Contenido sobre el grano */
	body > * {
		position: relative;
		z-index: 2;
	}

	/* Estilo para secciones distintivas */
	section {
		margin: 2rem 0;
		padding: 1rem;
		border: 1px solid var(--color-section-border);
		border-radius: 1rem;
		background: linear-gradient(
			rgba(147, 51, 234, 0.05),
			rgba(147, 51, 234, 0.02)
		);
		backdrop-filter: blur(8px);
	}

	h1, h2, h3, h4, h5, h6 {
		line-height: 1.2;
		font-weight: 600;
		color: var(--color-secondary);
	}

	p {
		line-height: 1.6;
	}

	a {
		color: var(--color-secondary);
		text-decoration: none;
		transition: all 0.3s ease;
	}

	a:hover {
		opacity: 0.8;
	}
</style>